spring:
  datasource:
    url: ${DATABASE_JDBC_URL}
    username: ${DATABASE_POOLER_USER}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
    open-in-view: false

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: 86400000  # 24 saat

supabase:
  url: https://wzcxopnfjguisxuclwjs.supabase.co
  anon-key: ${SUPABASE_ANON_KEY}

server:
  port: 8080

cors:
  allowed-origins: http://localhost:5173
